server:
  port: 8080
  
        
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://125.128.10.133:3306/SCDB?serverTimezone=Asia/Seoul
    username: root
    password: chfhrtor9
    
  jpa:
    show-sql: true
    hibernate:
      format_sql: true
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
 
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type: trace